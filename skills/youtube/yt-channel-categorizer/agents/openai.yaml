interface:
  display_name: "YouTube Channel Categorizer"
  short_description: "Fetch and categorize YouTube channels"
  default_prompt: "Use $yt-channel-categorizer to categorize these YouTube channel IDs."
policy:
  allow_implicit_invocation: true
