interface:
  display_name: "Format Music Release"
  short_description: "Tag and format music releases from metadata"
  default_prompt: "Use $format-music-release to tag and format this release or file path: <path>"
policy:
  allow_implicit_invocation: false
