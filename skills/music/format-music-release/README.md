# format-music-release

Format a music release folder or loose audio file using archive rules and verified release metadata — consistent embedded tags, artwork, track filenames, and archive naming.

## Requirements

Works best with:

```sh
brew install flac ffmpeg exiftool
```
